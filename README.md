# Matrix Official

Responsive static website implementation of the supplied Figma design.

Open `index.html` directly, or run a local server:

```powershell
python -m http.server 8000
```

Then visit `http://localhost:8000`.
